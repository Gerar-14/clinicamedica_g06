<% content_for :content do %>
  <p style="color: green"><%= notice %></p>

  <h1>Tipo Examen</h1>

  <% if @permisos_crud%>
  <%= link_to "Agregar tipo de examen", new_type_exam_path, :class => "btn btn-outline-success" %>
  <% end %>

  <div id="tipoexamen">
    <div class="card-body">
      <table id="datatablesSimple" class="table">
        <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Tipo Examen:</th>
          <th scope="col">Acciones</th>
        </tr>
        </thead>
        <tbody>
        <div hidden="true"><%= @number = 1 %></div>
        <% @type_exams.each do |type_exam| %>
          <tr>
            <th scope="row"><%= @number %></th>
            <td><%= type_exam.name_type_examn %></td>
            <td><%= link_to "Ver", type_exam, :class => "btn btn-outline-primary" %></td>
            <div hidden="true"><%= @number = @number + 1 %></div>
          </tr>
          <%#= render country %>
          <!--              <p><%#= link_to "Ver", muestra, :class=>"btn btn-outline-primary" %></p>-->
        <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <%#= link_to "Agregar Pais", new_country_path %>


<% end %>
<%= render template: "home/nav" %>
<%= render template: "layouts/base" %>
