<%= form_with(model: type_exam) do |form| %>
  <% if type_exam.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_exam.errors.count, "error") %> prohibited this type_exam from being saved:</h2>

      <ul>
        <% type_exam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-6">
    <%= form.label :"Tipo de examen", style: "display: block" %>
    <%= form.text_field :name_type_examn, class:"form-control" %>
  </div>

  <br>
  <div>
    <%= form.submit "Guardar", class:"btn btn-outline-success"%>
    <%= link_to "Cancelar", type_exams_path, :class => "btn btn-outline-danger"%>
  </div>
<% end %>



