<p style="color: green"><%= notice %></p>

<h1>Menus</h1>
<h2>que ondas desde index.html.erb</h2>

<div id="menus">
  <% @menus.each do |menu| %>
    <%= render menu %>
    <p>
      <%= link_to "Show this menu", menu %>
    </p>
  <% end %>
</div>

<%= link_to "New menu", new_menu_path %>

<h1>Accesos</h1>

<div id="accesos">
  <% @accesos.each do |acceso| %>
    <%= render acceso %>
    
  <% end %>
</div>

