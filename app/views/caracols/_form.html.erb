<%= form_with(model: caracol) do |form| %>
  <% if caracol.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(caracol.errors.count, "error") %> prohibited this caracol from being saved:</h2>

      <ul>
        <% caracol.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
