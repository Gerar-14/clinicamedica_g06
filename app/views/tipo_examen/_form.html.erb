<%= form_with(model: tipo_examen) do |form| %>
  <% if tipo_examen.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_examen.errors.count, "error") %> prohibited this tipo_examen from being saved:</h2>

      <ul>
        <% tipo_examen.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre_tipo_examen, style: "display: block" %>
    <%= form.text_field :nombre_tipo_examen %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
