<%= form_with(model: tipo_examan) do |form| %>
  <% if tipo_examan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_examan.errors.count, "error") %> prohibited this tipo_examan from being saved:</h2>

      <ul>
        <% tipo_examan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre_tipo_examen, style: "display: block" %>
    <%= form.text_field :nombre_tipo_examen %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
