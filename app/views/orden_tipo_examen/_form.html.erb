<%= form_with(model: orden_tipo_examen) do |form| %>
  <% if orden_tipo_examen.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(orden_tipo_examen.errors.count, "error") %> prohibited this orden_tipo_examen from being saved:</h2>

      <ul>
        <% orden_tipo_examen.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo_examen_id, style: "display: block" %>
    <%= form.text_field :tipo_examen_id %>
  </div>

  <div>
    <%= form.label :orden_id, style: "display: block" %>
    <%= form.text_field :orden_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
