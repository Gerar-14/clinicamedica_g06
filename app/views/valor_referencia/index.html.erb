<p style="color: green"><%= notice %></p>

<h1>Valor referencia</h1>

<div id="valor_referencia">
  <% @valor_referencia.each do |valor_referencium| %>
    <%= render valor_referencium %>
    <p>
      <%= link_to "Show this valor referencia", valor_referencium %>
    </p>
  <% end %>
</div>

<%= link_to "New valor referencia", new_valor_referencium_path %>
