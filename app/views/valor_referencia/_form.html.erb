<%= form_with(model: valor_referencium) do |form| %>
  <% if valor_referencium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(valor_referencium.errors.count, "error") %> prohibited this valor_referencium from being saved:</h2>

      <ul>
        <% valor_referencium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :valor_referencia, style: "display: block" %>
    <%= form.text_field :valor_referencia %>
  </div>

  <div>
    <%= form.label :parametro_id, style: "display: block" %>
    <%= form.text_field :parametro_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
