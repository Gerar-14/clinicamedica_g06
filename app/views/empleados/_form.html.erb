<%= form_with(model: empleado) do |form| %>
  <% if empleado.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(empleado.errors.count, "error") %> prohibited this empleado from being saved:</h2>

      <ul>
        <% empleado.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-row">
    <div class="form-group col-md-4">
      <%= form.label :nombre_empleado, style: "display: block" %>
      <%= form.text_field :nombre_empleado, class: "form-control" %>
    </div>

    <div class="form-group col-md-4">
      <%= form.label :apellido_empleado, style: "display: block" %>
      <%= form.text_field :apellido_empleado, class: "form-control" %>
    </div>
  </div>

  <div class="form-row">
    <div class="form-group col-md-4">
      <%= form.label :"Profesión", style: "display: block" %>
      <%= form.text_field :profesion, class: "form-control" %>
    </div>

    <div class="form-group col-md-4">
      <%= form.label :dui_empleado, style: "display: block" %>
      <%= form.text_field :dui_empleado, class: "form-control" %>
    </div>
  </div>

  <div class="form-row">
    <div class="form-group col-md-4">
      <%= form.label :municipio_id, style: "display: block" %>
      <%= form.collection_select :municipio_id, Municipio.all,:id, :nombre_municipio,{prompt: "Seleccione un municipio"}, {:id => 'simple-example'} %>
    </div>

    <div hidden= true>
      <%= form.label :users_id, style: "display: block" %>
      <%= form.text_field :user_id, value: current_user.id, class: "form-control" %>
    </div>
  </div>

  <br>
  <div>
    <%= form.submit "Guardar", class: "btn btn-outline-success" %>
    <%= link_to "Cancelar", empleados_path, :class => "btn btn-outline-danger" %>
  </div>

  <% content_for :js do %>
    <script>
        $(document).ready(function () {
            $('#simple-example').select2({ width: '40%' });
            $('#simple-example2').select2();
        });
    </script>
  <% end %>

<% end %>
