<%= form_with(model: tipo_examen_parametro) do |form| %>
  <% if tipo_examen_parametro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_examen_parametro.errors.count, "error") %> prohibited this tipo_examen_parametro from being saved:</h2>

      <ul>
        <% tipo_examen_parametro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :parametro_id, style: "display: block" %>
    <%= form.text_field :parametro_id %>
  </div>

  <div>
    <%= form.label :tipo_examen_id, style: "display: block" %>
    <%= form.text_field :tipo_examen_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
