<%= form_with(model: area_type_exam) do |form| %>
  <% if area_type_exam.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(area_type_exam.errors.count, "error") %> prohibited this area_type_exam from being saved:</h2>

      <ul>
        <% area_type_exam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :area_id, style: "display: block" %>
    <%= form.text_field :area_id %>
  </div>

  <div>
    <%= form.label :type_exam_id, style: "display: block" %>
    <%= form.text_field :type_exam_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
