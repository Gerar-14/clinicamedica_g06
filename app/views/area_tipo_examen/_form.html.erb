<%= form_with(model: area_tipo_examan) do |form| %>
  <% if area_tipo_examan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(area_tipo_examan.errors.count, "error") %> prohibited this area_tipo_examan from being saved:</h2>

      <ul>
        <% area_tipo_examan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :area_id, style: "display: block" %>
    <%= form.text_field :area_id %>
  </div>

  <div>
    <%= form.label :tipo_examen_id, style: "display: block" %>
    <%= form.text_field :tipo_examen_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
