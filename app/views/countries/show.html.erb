<% content_for :content do %>
  <p style="color: green"><%= notice %></p>

  <%= render @country %>

  <div>
    <%= link_to "Editar país", edit_country_path(@country), :class => "btn btn-outline-warning" %> |
    <%= link_to "Regresar", countries_path, :class => "btn btn-outline-dark" %>

    <%= button_to "Eliminar", @country, method: :delete, :class => "btn btn-outline-danger" %>
  </div>
<% end %>
<%= render template: "layouts/base" %>
