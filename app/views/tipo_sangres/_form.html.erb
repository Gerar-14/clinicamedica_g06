<%= form_with(model: tipo_sangre) do |form| %>
  <% if tipo_sangre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_sangre.errors.count, "error") %> prohibited this tipo_sangre from being saved:</h2>

      <ul>
        <% tipo_sangre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-6">
    <%= form.label :nombre_tipo_sangre, style: "display: block" %>
    <%= form.text_field :nombre_tipo_sangre, class:"form-control"  %>
  </div>

  <br>
  <div>
    <%= form.submit "Guardar", class:"btn btn-outline-success"%>
    <%= link_to "Cancelar", tipo_sangres_path, :class => "btn btn-outline-danger"%>
  </div>
<% end %>


