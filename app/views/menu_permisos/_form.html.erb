<%= form_with(model: menu_permiso) do |form| %>
  <% if menu_permiso.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(menu_permiso.errors.count, "error") %> prohibited this menu_permiso from being saved:</h2>

      <ul>
        <% menu_permiso.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-6">
    <%= form.label :"Menú", style: "display: block" %>
    <%= form.collection_select :menu_id, Menu.all,:id, :nombre %>
  </div>

  <div class="form-group col-md-6">
    <%= form.label :permiso_id, style: "display: block" %>
    <%= form.collection_select :permiso_id, Permiso.all,:id, :nombre_permiso %>
  </div>

  <br>
  <div>
    <%= form.submit "Guardar", class:"btn btn-outline-success" %>
    <%= link_to "Cancelar", menu_permisos_path, :class => "btn btn-outline-danger" %>
  </div>
<% end %>
