<%= form_with(model: orden) do |form| %>
  <% if orden.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(orden.errors.count, "error") %> prohibited this orden from being saved:</h2>

      <ul>
        <% orden.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :fecha_examen, style: "display: block" %>
    <%= form.datetime_field :fecha_examen %>
  </div>

  <div>
    <%= form.label :paciente_id, style: "display: block" %>
    <%= form.text_field :paciente_id %>
  </div>

  <div>
    <%= form.label :laboratorista_id, style: "display: block" %>
    <%= form.text_field :laboratorista_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
