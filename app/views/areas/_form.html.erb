<%= form_with(model: area) do |form| %>
  <% if area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
        <% area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-6">
    <%= form.label :nombre_area, style: "display: block" %>
    <%= form.text_field :nombre_area, class:"form-control" %>
  </div>

  <br>

  <div>
    <%= form.submit "Guardar", class:"btn btn-outline-success"%>
    <%= link_to "Cancelar", areas_path, :class => "btn btn-outline-danger"%>
  </div>

<% end %>
