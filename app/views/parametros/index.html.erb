<% content_for :content do %>
  <p style="color: green"><%= notice %></p>

  <h1>Parametros</h1>

  <%= link_to "Agregar parametro", new_parametro_path, :class => "btn btn-outline-success" %>

  <div id="parametros">
    <div class="card-body">
      <table id="datatablesSimple" class="table">
        <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Parametro:</th>
          <th scope="col">Acciones</th>
        </tr>
        </thead>
        <tbody>

        <% @parametros.each do |parametro| %>
          <tr>
            <th scope="row">1</th>
            <td><%= parametro.nombre_parametro %></td>
            <td><%= link_to "Ver", parametro, :class => "btn btn-outline-primary" %></td>
          </tr>
          <%#= render country %>
          <!--              <p><%#= link_to "Ver", muestra, :class=>"btn btn-outline-primary" %></p>-->
        <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <%#= link_to "Agregar Pais", new_country_path %>


<% end %>
<%= render template: "layouts/base" %>
